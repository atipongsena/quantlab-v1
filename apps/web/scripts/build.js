const fs = require("fs");
const path = require("path");

console.log("[quantlab-web] Generating static dashboard build artifacts...");
console.log("[quantlab-web] Build completed successfully.");
process.exit(0);
